// utils.cpp (或者在你的 main 函數所在的 .cpp 文件中)
#include "utils.h" // 包含頭文件以聲明這些變數

struct timeval start;
struct timeval end_;
unsigned long diff;

float rx_gate_time_ms;

float csqs_time_ms;

float transmission_time_ms;
